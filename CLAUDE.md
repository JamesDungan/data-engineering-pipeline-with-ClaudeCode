# Project: data-engineering-pipeline-with-ClaudeCode

## Scope — MVP ONLY
Pipeline: CSV → raw table → transform → 3 metrics → Streamlit dashboard.

- Implement ONLY what the current task asks for.
- No auth, schedulers, cloud services, or extra pages.
- Do not add abstractions, config options, or scaffolding for future features not yet requested.

## Output format
After making changes, always report:
1. What changed
2. Files touched
3. How to run locally
4. Quick verification step

Be descriptive in explanations — this is a learning project.

## Commits
Commit after each major step with a clear, descriptive message (not just at the end of a session).
